---
phase: 6.1
slug: restructure-into-payload-toolkit-turborepo-monorepo
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-15
---

# Phase 6.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | TypeScript compilation + turbo build |
| **Config file** | turbo.json, pnpm-workspace.yaml |
| **Quick run command** | `turbo build` |
| **Full suite command** | `turbo build && turbo check` |
| **Estimated runtime** | ~60 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npx tsc --noEmit` in relevant package
- **After every plan wave:** Run `turbo build`
- **Max feedback latency:** 60 seconds

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| CLI scaffolds from monorepo | Restructure | Requires GitHub repo + network | Run npx create-payload-starter, verify template extracted |
| Shared package imported correctly | Shared utils | Requires build + runtime | CLI uses shared create-database, verify it works |
| apps/starter builds independently | Template | Requires running build | cd apps/starter && pnpm build |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify
- [ ] Sampling continuity maintained
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
