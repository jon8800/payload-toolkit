---
phase: 07-gap-closure-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/starter/.env.example
  - apps/starter/scripts/seed-demo.ts
  - apps/starter/scripts/setup.ts
  - apps/starter/src/blocks/registry.ts
  - apps/starter/src/blocks/generateBlocks.ts
  - apps/starter/src/utilities/deepMerge.ts
autonomous: true
requirements: [DX-02, DX-04]
gap_closure: true

must_haves:
  truths:
    - "ADMIN_EMAIL is documented in .env.example with a comment explaining its purpose"
    - "All 8 section presets are used in the seed-demo script to produce richer demo content"
    - "No orphaned utilities or unused exports remain in the codebase"
    - "setup.ts references @jon8800 scope instead of @yourscope placeholder"
  artifacts:
    - path: "apps/starter/.env.example"
      provides: "Complete env var documentation including ADMIN_EMAIL"
      contains: "ADMIN_EMAIL"
    - path: "apps/starter/scripts/seed-demo.ts"
      provides: "Demo seeder using all 8 section presets"
      contains: "collectionGridPreset"
    - path: "apps/starter/scripts/setup.ts"
      provides: "Redirect message with correct package scope"
      contains: "@jon8800"
  key_links:
    - from: "apps/starter/scripts/seed-demo.ts"
      to: "apps/starter/src/data/section-presets/index.ts"
      via: "import of all 8 presets"
      pattern: "collectionGridPreset"
---

<objective>
Fix missing env documentation, wire unused seed presets, and clean up dead code identified in the v1.0 milestone audit.

Purpose: Close DX-02 (unused seed presets) and DX-04 (ADMIN_EMAIL undocumented) gaps, plus remove tech debt (orphaned code, placeholder references).
Output: Cleaner codebase with all presets utilized and complete env documentation.
</objective>

<execution_context>
@C:/Users/Eugene/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eugene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/starter/.env.example
@apps/starter/scripts/seed-demo.ts
@apps/starter/scripts/setup.ts
@apps/starter/src/blocks/registry.ts
@apps/starter/src/blocks/generateBlocks.ts
@apps/starter/src/utilities/deepMerge.ts
@apps/starter/src/data/section-presets/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ADMIN_EMAIL to .env.example and fix setup.ts placeholder</name>
  <files>apps/starter/.env.example, apps/starter/scripts/setup.ts</files>
  <action>
1. In `apps/starter/.env.example`, add an `ADMIN_EMAIL` entry in the SMTP section (after the SMTP vars, before any closing comment). Add it as a commented-out optional var with explanation:

```
# Fallback recipient for form submission notifications (used when no email is configured on the form)
# ADMIN_EMAIL=admin@yourdomain.com
```

2. In `apps/starter/scripts/setup.ts`, replace all instances of `@yourscope` with `@jon8800`:
   - `@yourscope/create-payload-starter` becomes `@jon8800/create-payload-starter`
   - `https://github.com/yourscope/create-payload-starter` becomes `https://github.com/jon8800/payload-toolkit`
  </action>
  <verify>
    <automated>grep -c "ADMIN_EMAIL" apps/starter/.env.example && grep -c "@jon8800" apps/starter/scripts/setup.ts && echo "PASS"</automated>
  </verify>
  <done>ADMIN_EMAIL documented in .env.example with purpose comment. setup.ts references correct @jon8800 scope and repo URL.</done>
</task>

<task type="auto">
  <name>Task 2: Wire unused presets into seed-demo and clean dead code</name>
  <files>apps/starter/scripts/seed-demo.ts, apps/starter/src/blocks/registry.ts, apps/starter/src/blocks/generateBlocks.ts, apps/starter/src/utilities/deepMerge.ts</files>
  <action>
**Seed preset wiring (DX-02):**

In `apps/starter/scripts/seed-demo.ts`:

1. Add `collectionGridPreset` to the imports from `@/data/section-presets` (faqPreset and footerCtaPreset are already imported)

2. Use the 3 unused presets in the demo pages:
   - **Contact page:** After the existing contactContent, add an FAQ section:
     ```typescript
     const contactFaq = populatePresetContent(faqPreset.blocks, {
       paragraph: 'Check our FAQ below for quick answers to common questions about our platform and services.',
     })
     ```
     Add `...contactFaq` to the Contact page layout array after `...contactContent`

   - **Home page:** Add a collectionGrid section before the CTA. Insert between homeFeatures and homeCta:
     ```typescript
     const homeGrid = populatePresetContent(collectionGridPreset.blocks, {
       paragraph: 'Explore our latest articles and resources to help you get the most out of the platform.',
     })
     ```
     Update the Home page layout to: `[...homeHero, ...homeFeatures, ...homeGrid, ...homeCta]`

   - **About page:** Add a footerCta section after testimonials:
     ```typescript
     const aboutCta = populatePresetContent(footerCtaPreset.blocks, {
       paragraph: 'Ready to build your next project? Get started with our composable block system today.',
     })
     ```
     Update the About page layout to: `[...aboutContent, ...aboutTestimonials, ...aboutCta]`

**Dead code cleanup:**

3. In `apps/starter/src/blocks/registry.ts`:
   - Remove the unused `generateBlocks` import (keep the `type RecursiveBlock` import since it's used by `leafBlockFactories` and `layoutBlockFactories` type annotations)
   - Remove the re-export lines: `export { atomicBlockSlugs } from './slugs'` and `export type { AtomicBlockSlug } from './slugs'`
   - Remove the `sectionBlockSlugs` array and its reference in `allBlockSlugs` (it's always empty, sections are data presets not block configs)

4. In `apps/starter/src/blocks/generateBlocks.ts`:
   - Remove the `generateBlocks` function entirely (lines 27-82). It is never called -- registry.ts has its own inline `generateLayoutBlocks`.
   - Keep the `RecursiveBlock` type export and `getBaseBlockSlug` function (both are used elsewhere).
   - Remove the `MAX_DEPTH` constant (only used by the removed function).

5. Delete `apps/starter/src/utilities/deepMerge.ts` entirely -- it has zero consumers in the codebase (orphaned template utility).
  </action>
  <verify>
    <automated>grep -c "collectionGridPreset" apps/starter/scripts/seed-demo.ts && grep -c "faqPreset" apps/starter/scripts/seed-demo.ts && ! test -f apps/starter/src/utilities/deepMerge.ts && echo "PASS"</automated>
  </verify>
  <done>All 8 section presets used in seed-demo.ts. generateBlocks function removed (type kept). atomicBlockSlugs re-export removed. sectionBlockSlugs removed. deepMerge.ts deleted. No orphaned code remains.</done>
</task>

</tasks>

<verification>
- `grep "ADMIN_EMAIL" apps/starter/.env.example` shows the env var entry
- `grep "@yourscope" apps/starter/scripts/setup.ts` returns no results
- `grep "collectionGridPreset\|faqPreset\|footerCtaPreset" apps/starter/scripts/seed-demo.ts` shows all 3 used in actual page layouts
- `test -f apps/starter/src/utilities/deepMerge.ts` returns false (file deleted)
- `grep "generateBlocks" apps/starter/src/blocks/registry.ts` shows no function import (only type import)
- `grep "atomicBlockSlugs" apps/starter/src/blocks/registry.ts` returns no results
- `grep "sectionBlockSlugs" apps/starter/src/blocks/registry.ts` returns no results
</verification>

<success_criteria>
- ADMIN_EMAIL documented in .env.example
- setup.ts references @jon8800 correctly
- All 8 section presets produce demo content in seed-demo.ts
- No orphaned deepMerge.ts utility
- No unused generateBlocks function in generateBlocks.ts
- No unused atomicBlockSlugs re-export or sectionBlockSlugs in registry.ts
</success_criteria>

<output>
After completion, create `.planning/phases/07-gap-closure-cleanup/07-02-SUMMARY.md`
</output>
