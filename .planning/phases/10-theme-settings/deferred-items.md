# Deferred Items - Phase 10

## Pre-existing TypeScript Errors (Out of Scope)

1. **revalidateTheme.ts:25,40** - `revalidateTag` requires 2 arguments in Next.js 16 (missing `profile` param). Affects lines 25 and 40.
2. **DocumentFields.tsx:73** - Implicit `any` type on `config.blocksMap[block]` index expression.
