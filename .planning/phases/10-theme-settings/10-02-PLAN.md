---
phase: 10-theme-settings
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/starter/src/fields/theme/ColorPicker.tsx
  - apps/starter/src/fields/theme/FontSelector.tsx
  - apps/starter/src/fields/theme/SliderField.tsx
autonomous: true
requirements: [THEME-02]

must_haves:
  truths:
    - "Admin can pick a color using a visual color picker and see the hex value in a text input"
    - "Admin can search and select a Google Font family for sans and mono"
    - "Admin can adjust spacing and border radius using a range slider with numeric display"
    - "Field values persist when the global is saved and reloaded"
  artifacts:
    - path: "apps/starter/src/fields/theme/ColorPicker.tsx"
      provides: "Color picker + hex text input field component for Payload admin"
      exports: ["ColorPickerField"]
    - path: "apps/starter/src/fields/theme/FontSelector.tsx"
      provides: "Google Fonts search/select field component for Payload admin"
      exports: ["FontSelectorField"]
    - path: "apps/starter/src/fields/theme/SliderField.tsx"
      provides: "Range slider + number input field component for Payload admin"
      exports: ["SliderField"]
  key_links:
    - from: "apps/starter/src/globals/ThemeSettings.ts"
      to: "apps/starter/src/fields/theme/ColorPicker.tsx"
      via: "admin.components.Field path string"
      pattern: "@/fields/theme/ColorPicker#ColorPickerField"
    - from: "apps/starter/src/globals/ThemeSettings.ts"
      to: "apps/starter/src/fields/theme/FontSelector.tsx"
      via: "admin.components.Field path string"
      pattern: "@/fields/theme/FontSelector#FontSelectorField"
    - from: "apps/starter/src/globals/ThemeSettings.ts"
      to: "apps/starter/src/fields/theme/SliderField.tsx"
      via: "admin.components.Field path string"
      pattern: "@/fields/theme/SliderField#SliderField"
---

<objective>
Create three custom Payload admin field components for visual theme editing: a color picker with hex input, a Google Fonts search/select dropdown, and a range slider with numeric display.

Purpose: These components replace raw text/number inputs with visual controls, making theme editing intuitive for non-technical admins. They wire into the ThemeSettings global fields created in Plan 01.
Output: Three client-side React components registered as Payload custom field components.
</objective>

<execution_context>
@C:/Users/Eugene/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eugene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-theme-settings/10-CONTEXT.md
@.planning/phases/10-theme-settings/10-RESEARCH.md
@.planning/phases/10-theme-settings/10-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: ThemeSettings global field structure the components plug into -->

From apps/starter/src/globals/ThemeSettings.ts (created in Plan 01):
```typescript
// Color fields use:
admin: {
  components: {
    Field: '@/fields/theme/ColorPicker#ColorPickerField',
  },
}

// Font fields use:
admin: {
  components: {
    Field: '@/fields/theme/FontSelector#FontSelectorField',
  },
}

// Spacing/radius fields use:
admin: {
  components: {
    Field: '@/fields/theme/SliderField#SliderField',
  },
}

// Slider fields pass config via admin.custom:
admin: {
  custom: { min: 1, max: 16, step: 1 } // spacing
  // or
  custom: { min: 0, max: 2, step: 0.125, unit: 'rem' } // borderRadius
}
```

Payload custom field component pattern:
```typescript
'use client'
import { useField } from '@payloadcms/ui'
import type { TextFieldClientComponent } from 'payload'

export const MyField: TextFieldClientComponent = ({ path, field }) => {
  const { value, setValue } = useField<string>({ path })
  // field.admin?.custom contains custom props
  return (/* ... */)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ColorPicker and SliderField components</name>
  <files>apps/starter/src/fields/theme/ColorPicker.tsx, apps/starter/src/fields/theme/SliderField.tsx</files>
  <action>
1. Create `apps/starter/src/fields/theme/ColorPicker.tsx`:
   - Add `'use client'` directive at top
   - Import `useField` from `'@payloadcms/ui'` and `TextFieldClientComponent` from `'payload'`
   - Export `ColorPickerField` as a named export implementing `TextFieldClientComponent`
   - Component receives `{ path, field }` props
   - Uses `useField<string>({ path })` to get `value` and `setValue`
   - Renders a row with:
     a. `<input type="color">` — value bound to `value || '#000000'`, onChange calls `setValue(e.target.value)`
     b. `<input type="text">` — value bound to `value || ''`, onChange calls `setValue(e.target.value)`, placeholder `#000000`, pattern for hex validation
     c. A small color swatch preview div showing the current color as background
   - Display the field label from `field.label` (use `typeof field.label === 'string' ? field.label : path` for safety)
   - Show `field.admin?.description` below if present
   - Style with Tailwind classes: flex row, gap-2, items-center. The color input should be size-10 with cursor-pointer. The text input should have standard Payload-like styling (border, rounded, px-3 py-2, text-sm). The swatch preview is size-8 rounded border.
   - Use standard function declaration per project conventions

2. Create `apps/starter/src/fields/theme/SliderField.tsx`:
   - Add `'use client'` directive at top
   - Import `useField` from `'@payloadcms/ui'` and `NumberFieldClientComponent`, `TextFieldClientComponent` from `'payload'`
   - Export `SliderField` as a named export
   - The component must handle both number fields (spacing) and text fields (borderRadius). Use a union type or just `TextFieldClientComponent` since Payload will pass the field regardless.
   - Read config from `field.admin?.custom` — extract `min`, `max`, `step`, `unit` (default unit to empty string)
   - Uses `useField<string>({ path })` to get value/setValue (text field stores string, number field stores number — handle both by converting)
   - Renders a row with:
     a. `<input type="range">` — min/max/step from custom config, value as number
     b. `<input type="number">` — same min/max/step, compact width (w-20)
     c. Unit label span if `unit` is set (e.g., "rem")
   - Both inputs are synced: changing either updates the field value via setValue
   - Display field label and description
   - Style: flex row, gap-3, items-center. Range input gets flex-1, accent-primary. Number input gets w-20 border rounded px-2 py-1 text-sm.
   - Use standard function declaration
  </action>
  <verify>
    <automated>cd apps/starter && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>ColorPickerField renders a native color picker + hex text input that syncs values bidirectionally via useField. SliderField renders a range slider + number input with configurable min/max/step/unit via field.admin.custom. Both compile without TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create FontSelector component with Google Fonts search</name>
  <files>apps/starter/src/fields/theme/FontSelector.tsx</files>
  <action>
1. Create `apps/starter/src/fields/theme/FontSelector.tsx`:
   - Add `'use client'` directive at top
   - Import `useField` from `'@payloadcms/ui'`, `TextFieldClientComponent` from `'payload'`
   - Import font data: `import fonts from 'google-font-metadata'` (this is a static JSON object keyed by font family name). If the import shape is different (check the package — it may export `fontsData` or require a subpath like `google-font-metadata/data/google-fonts-v2.json`), adjust accordingly. The key thing is getting a list of font family names.
   - Export `FontSelectorField` as named export implementing `TextFieldClientComponent`
   - Component state:
     a. `searchQuery` (string) — controlled text input for searching
     b. `isOpen` (boolean) — whether dropdown is visible
     c. `filteredFonts` (string[]) — computed from search query
   - Derive font family names list from the google-font-metadata import (Object.keys(fonts) or similar). Memoize with useMemo.
   - Filter logic: case-insensitive includes match on searchQuery, limit results to 20 for performance
   - Renders:
     a. Field label and description
     b. Current value display (if set): show the font family name with a "clear" button (x icon or text)
     c. Search input: text input with placeholder "Search Google Fonts...", onFocus opens dropdown, onChange updates searchQuery and filters
     d. Dropdown list: absolute positioned div below the input, showing filtered font names as clickable items. Each item onClick calls `setValue(fontFamily)` and closes dropdown
     e. Click outside handler: useEffect with document click listener to close dropdown
   - Style: relative container for dropdown positioning. Search input: w-full border rounded px-3 py-2 text-sm. Dropdown: absolute top-full left-0 w-full max-h-60 overflow-y-auto bg-white border rounded shadow-lg z-50. Items: px-3 py-2 hover:bg-muted cursor-pointer text-sm.
   - Use standard function declaration

Note on google-font-metadata: The package exports a large JSON object. If the default import doesn't work as expected at build time, fall back to a simpler approach: create a static list of ~50 popular Google Fonts as a constant array in the component file, with a comment noting that google-font-metadata can be integrated for the full list. The important thing is that font selection WORKS — the full 1500+ font list is an enhancement.
  </action>
  <verify>
    <automated>cd apps/starter && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>FontSelectorField renders a searchable dropdown of Google Font families. Selecting a font sets the field value to the font family name string. Search filters the list case-insensitively. Dropdown closes on selection or click outside. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes: `cd apps/starter && npx tsc --noEmit`
2. All three components use `'use client'` directive
3. All three use `useField` from `@payloadcms/ui` for form integration
4. Export names match the `#ComponentName` references in ThemeSettings.ts field configs
5. Components use standard function declarations (not arrow functions)
</verification>

<success_criteria>
- ColorPickerField: native color picker + hex text input, synced, shows label/description
- SliderField: range slider + number input, reads min/max/step/unit from field.admin.custom
- FontSelectorField: searchable Google Fonts dropdown, sets font family name as value
- All three components compile and match the export names referenced in ThemeSettings global config
</success_criteria>

<output>
After completion, create `.planning/phases/10-theme-settings/10-02-SUMMARY.md`
</output>
