---
phase: 06-developer-experience
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Projects/sandbox/create-payload-starter/package.json
  - C:/Projects/sandbox/create-payload-starter/tsconfig.json
  - C:/Projects/sandbox/create-payload-starter/src/index.ts
  - C:/Projects/sandbox/create-payload-starter/src/prompts.ts
  - C:/Projects/sandbox/create-payload-starter/src/scaffold.ts
  - C:/Projects/sandbox/create-payload-starter/src/render-templates.ts
  - C:/Projects/sandbox/create-payload-starter/src/post-scaffold.ts
  - C:/Projects/sandbox/create-payload-starter/src/lib/create-database.ts
  - C:/Projects/sandbox/create-payload-starter/src/lib/generate-env.ts
  - C:/Projects/sandbox/create-payload-starter/templates/payload.config.ts.ejs
  - C:/Projects/sandbox/create-payload-starter/templates/Posts.ts.ejs
  - C:/Projects/sandbox/create-payload-starter/templates/TemplateParts.ts.ejs
autonomous: true
gap_closure: true
requirements: [DX-01]

must_haves:
  truths:
    - "CLI package exists as a separate project at C:/Projects/sandbox/create-payload-starter/"
    - "EJS templates produce clean TypeScript output with no commented-out code or @setup markers"
    - "Collection dependencies are enforced: selecting Posts force-includes Categories and Tags"
    - "Plugin configs that reference optional collections have conditional collection arrays"
  artifacts:
    - path: "C:/Projects/sandbox/create-payload-starter/package.json"
      provides: "CLI package manifest with bin entry"
      contains: "bin"
    - path: "C:/Projects/sandbox/create-payload-starter/templates/payload.config.ts.ejs"
      provides: "EJS template for payload config with conditional collections/plugins"
      contains: "<%"
    - path: "C:/Projects/sandbox/create-payload-starter/templates/Posts.ts.ejs"
      provides: "EJS template for Posts collection with conditional relationship fields"
      contains: "<%"
    - path: "C:/Projects/sandbox/create-payload-starter/templates/TemplateParts.ts.ejs"
      provides: "EJS template for TemplateParts with conditional display conditions"
      contains: "<%"
    - path: "C:/Projects/sandbox/create-payload-starter/src/index.ts"
      provides: "CLI entry point orchestrating prompts -> scaffold -> render -> post-scaffold"
  key_links:
    - from: "src/index.ts"
      to: "src/prompts.ts"
      via: "import"
      pattern: "import.*prompts"
    - from: "src/index.ts"
      to: "src/scaffold.ts"
      via: "import"
      pattern: "import.*scaffold"
    - from: "src/render-templates.ts"
      to: "templates/*.ejs"
      via: "ejs.renderFile"
      pattern: "ejs\\.renderFile"
---

<objective>
Create the `create-payload-starter` CLI package with EJS templates, interactive prompts, scaffolding logic, and post-scaffold steps (DB creation, env generation, install, migrate, admin, git init).

Purpose: Replaces the in-place setup script with a proper CLI package that scaffolds into a new directory using EJS templates for clean conditional output. Fixes both UAT blockers: (1) scaffolds into new directory instead of modifying starter in-place, (2) uses templates with dependency-aware collection handling instead of comment-toggling.

Output: A complete, buildable CLI package at `C:/Projects/sandbox/create-payload-starter/` with all source files, templates, and configuration.
</objective>

<execution_context>
@C:/Users/Eugene/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eugene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-developer-experience/06-CONTEXT.md
@.planning/phases/06-developer-experience/06-UAT.md
@.planning/phases/06-developer-experience/06-01-SUMMARY.md
@.planning/phases/06-developer-experience/06-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts from the starter repo that the CLI must understand -->

From scripts/lib/create-database.ts (reuse this logic):
```typescript
type DatabaseConfig = {
  host: string
  port: number
  user: string
  password: string
  database: string
}
export async function createDatabase(config: DatabaseConfig): Promise<void>
```

From scripts/lib/generate-env.ts (reuse this logic, add targetDir):
```typescript
type GenerateEnvOptions = {
  projectName: string
  dbHost: string
  dbPort: string
  dbUser: string
  dbPassword: string
  adminEmail?: string
  overwrite?: boolean
  smtp?: { host: string; port: string; user: string; pass: string; from: string }
}
export function generateEnv(options: GenerateEnvOptions): void
```

From scripts/setup.ts (reuse prompt structure):
```typescript
// @clack/prompts group with: name, dbHost, dbPort, dbUser, dbPassword,
// adminEmail, adminPassword, collections (multiselect), plugins (multiselect), smtpSetup (confirm)
// Conditional SMTP sub-group: host, port, user, pass, from
```

Current payload.config.ts structure (for EJS template):
- Imports: 7 plugin imports, 4 optional collection imports (Posts, Categories, Tags, TemplateParts)
- collections array: Users, Media, Pages (required) + Posts, Categories, Tags, TemplateParts (optional)
- plugins array: 7 plugins each with conditional collection arrays
- smtpAdapter function conditionally applied
- Plugin configs reference optional collections in their arrays (e.g., SEO has ['pages', 'posts'], search has ['pages', 'posts', 'categories'])

Posts.ts fields (for EJS template):
- categories field: `relationTo: 'categories'`, hasMany: true (lines 92-97)
- tags field: `relationTo: 'tags'`, hasMany: true (lines 98-104)

TemplateParts.ts fields (for EJS template):
- displayCondition.collectionType options: 'pages' (always), 'posts' (conditional)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI package structure, prompts, and scaffold/render/post-scaffold modules</name>
  <files>
    C:/Projects/sandbox/create-payload-starter/package.json
    C:/Projects/sandbox/create-payload-starter/tsconfig.json
    C:/Projects/sandbox/create-payload-starter/.gitignore
    C:/Projects/sandbox/create-payload-starter/src/index.ts
    C:/Projects/sandbox/create-payload-starter/src/prompts.ts
    C:/Projects/sandbox/create-payload-starter/src/scaffold.ts
    C:/Projects/sandbox/create-payload-starter/src/render-templates.ts
    C:/Projects/sandbox/create-payload-starter/src/post-scaffold.ts
    C:/Projects/sandbox/create-payload-starter/src/lib/create-database.ts
    C:/Projects/sandbox/create-payload-starter/src/lib/generate-env.ts
  </files>
  <action>
    Create directory `C:/Projects/sandbox/create-payload-starter/` and all source files:

    **package.json:**
    - name: `@anthropic/create-payload-starter` (placeholder scope — user will change)
    - version: `0.1.0`
    - type: `module`
    - bin: `{ "create-payload-starter": "./dist/index.js" }`
    - scripts: `{ "build": "tsc", "dev": "tsx src/index.ts" }`
    - dependencies: `@clack/prompts`, `ejs`, `degit` (for template download), `pg`, `cross-spawn` (for cross-platform child_process)
    - devDependencies: `@types/ejs`, `@types/node`, `@types/pg`, `tsx`, `typescript`
    - Note: No `@types/degit` needed (untyped, use require or dynamic import)

    **tsconfig.json:**
    - target: ES2022, module: Node16, moduleResolution: Node16
    - outDir: ./dist, rootDir: ./src
    - strict: true, esModuleInterop: true, skipLibCheck: true

    **.gitignore:** node_modules, dist

    **src/index.ts** (CLI entry — `#!/usr/bin/env node` shebang):
    - Import and call `runPrompts()` from prompts.ts
    - Call `scaffoldProject(targetDir)` from scaffold.ts
    - Call `renderTemplates(targetDir, selections)` from render-templates.ts
    - Call `postScaffold(targetDir, selections)` from post-scaffold.ts
    - Wrap in try/catch with `p.cancel()` on error
    - Use `p.intro('Create Payload Starter')` and `p.outro('Done! ...')`

    **src/prompts.ts** — `export async function runPrompts()`:
    - Reuse the EXACT same prompt structure from `scripts/setup.ts` (project name, DB creds, admin, collections multiselect, plugins multiselect, SMTP optional)
    - CRITICAL CHANGE to collections multiselect: When Posts is selected, Categories and Tags must be force-included. Implementation: after the multiselect resolves, if `posts` is in selections, ensure `categories` and `tags` are also in selections (add them silently, log a note via `p.log.info`). The prompt hint for Posts should say "Posts (includes Categories & Tags)".
    - If `posts` is NOT selected, remove `categories` and `tags` from the options entirely (they only exist as Posts dependencies).
    - Return a typed selections object: `{ projectName, dbHost, dbPort, dbUser, dbPassword, adminEmail, adminPassword, collections: string[], plugins: string[], smtp?: { host, port, user, pass, from } }`

    **src/scaffold.ts** — `export async function scaffoldProject(targetDir: string)`:
    - Use `degit('user/payload-starter')` to download the starter template into targetDir. Note: The actual GitHub repo path is a placeholder — use `'payload-starter'` and add a comment that the user should update with their actual GitHub username/org.
    - Alternative approach (simpler for private repos): Use `git clone --depth 1` followed by removing the `.git` directory. Choose whichever is simpler. Use `cross-spawn` for cross-platform compatibility.
    - After download, delete files that shouldn't be in the generated project: `scripts/setup.ts`, `scripts/lib/modify-config.ts`, `.planning/` directory if present.
    - Do NOT delete: `scripts/lib/create-database.ts`, `scripts/lib/generate-env.ts`, `scripts/lib/lexical-helpers.ts`, `scripts/create-admin.ts`, `scripts/seed-demo.ts`, `Dockerfile`, `docker-compose.yml`, `.dockerignore`, `.env.example`

    **src/render-templates.ts** — `export async function renderTemplates(targetDir: string, selections)`:
    - Read each `.ejs` template from the `templates/` directory (resolved relative to the CLI package root using `import.meta.url`)
    - Render with EJS passing: `{ collections: string[], plugins: string[] }` as template data
    - Write rendered output to the correct path in targetDir:
      - `templates/payload.config.ts.ejs` -> `{targetDir}/src/payload.config.ts`
      - `templates/Posts.ts.ejs` -> `{targetDir}/src/collections/Posts.ts` (only if posts selected)
      - `templates/TemplateParts.ts.ejs` -> `{targetDir}/src/collections/TemplateParts.ts` (only if template-parts selected)
    - If posts NOT selected, delete `{targetDir}/src/collections/Posts.ts`, `Categories.ts`, `Tags.ts` and related hooks/frontend files: `src/hooks/revalidatePost.ts`, `src/app/(frontend)/blog/` directory
    - If template-parts NOT selected, delete `{targetDir}/src/collections/TemplateParts.ts` and `src/hooks/revalidateTemplatePart.ts`

    **src/post-scaffold.ts** — `export async function postScaffold(targetDir, selections)`:
    - Step 1: Generate `.env` in targetDir (copy the generateEnv logic but write to `path.join(targetDir, '.env')` — read `.env.example` from targetDir)
    - Step 2: Run `pnpm install` in targetDir via `cross-spawn.sync('pnpm', ['install'], { cwd: targetDir, stdio: 'inherit' })`
    - Step 3: Create database using the create-database logic (copy from starter's script)
    - Step 4: Run `pnpm payload migrate` in targetDir
    - Step 5: Create admin user: run `pnpm payload run scripts/create-admin.ts -- --email=X --password=Y` in targetDir
    - Step 6: `git init` + `git add -A` + `git commit -m "Initial commit from create-payload-starter"` in targetDir
    - Each step uses `p.spinner()` for progress feedback
    - Wrap each step in try/catch — non-critical steps (git init, admin creation) should warn but continue

    **src/lib/create-database.ts:** Copy from `payload-starter/scripts/lib/create-database.ts` verbatim.

    **src/lib/generate-env.ts:** Copy from `payload-starter/scripts/lib/generate-env.ts` but modify: accept a `targetDir` parameter, resolve paths relative to targetDir instead of `process.cwd()`.
  </action>
  <verify>
    <automated>cd C:/Projects/sandbox/create-payload-starter && npx tsc --noEmit</automated>
  </verify>
  <done>
    - CLI package compiles without TypeScript errors
    - All source modules exist with correct exports
    - package.json has bin entry and all dependencies declared
    - Prompts enforce Posts -> Categories+Tags dependency
    - Scaffold downloads template and cleans up setup-only files
    - Render function reads EJS templates and writes rendered output
    - Post-scaffold runs install, DB creation, migrate, admin, git init
  </done>
</task>

<task type="auto">
  <name>Task 2: EJS templates for payload.config.ts, Posts.ts, TemplateParts.ts</name>
  <files>
    C:/Projects/sandbox/create-payload-starter/templates/payload.config.ts.ejs
    C:/Projects/sandbox/create-payload-starter/templates/Posts.ts.ejs
    C:/Projects/sandbox/create-payload-starter/templates/TemplateParts.ts.ejs
  </files>
  <action>
    Create three EJS templates in `C:/Projects/sandbox/create-payload-starter/templates/`. Each template receives `{ collections: string[], plugins: string[] }` as data. The output must be **clean, valid TypeScript** — no commented-out code, no markers, no empty lines where removed code was.

    **templates/payload.config.ts.ejs:**
    Base the template on the current `src/payload.config.ts` but replace @setup markers with EJS conditionals.

    Imports section:
    ```
    <% if (plugins.includes('nested-docs')) { %>
    import { nestedDocsPlugin } from '@payloadcms/plugin-nested-docs'
    <% } %>
    <% if (plugins.includes('seo')) { %>
    import { seoPlugin } from '@payloadcms/plugin-seo'
    <% } %>
    ```
    (Same pattern for all 7 plugins)

    ```
    <% if (collections.includes('posts')) { %>
    import { Posts } from '@/collections/Posts'
    <% } %>
    <% if (collections.includes('categories')) { %>
    import { Categories } from '@/collections/Categories'
    <% } %>
    <% if (collections.includes('tags')) { %>
    import { Tags } from '@/collections/Tags'
    <% } %>
    <% if (collections.includes('template-parts')) { %>
    import { TemplateParts } from '@/collections/TemplateParts'
    <% } %>
    ```

    Collections array:
    ```
    collections: [
      Users,
      Media,
      Pages,
    <% if (collections.includes('posts')) { %>
      Posts,
    <% } %>
    <% if (collections.includes('categories')) { %>
      Categories,
    <% } %>
    <% if (collections.includes('tags')) { %>
      Tags,
    <% } %>
    <% if (collections.includes('template-parts')) { %>
      TemplateParts,
    <% } %>
    ],
    ```

    Plugins array — CRITICAL: Plugin configs that reference optional collections need conditional arrays:
    - `seoPlugin`: collections should be `[<% if (collections.includes('posts')) { %>'pages', 'posts'<% } else { %>'pages'<% } %>]`
    - `redirectsPlugin`: same pattern for its collections array
    - `nestedDocsPlugin`: only if both plugin AND categories selected (nested-docs only applies to categories)
    - `importExportPlugin`: conditional collections array based on what's selected
    - `mcpPlugin`: conditional collections object based on what's selected
    - `searchPlugin`: conditional collections array
    - `formBuilderPlugin`: always includes 'pages' in redirectRelationships, no optional collection dependency
    - Each plugin wrapped in `<% if (plugins.includes('plugin-name')) { %> ... <% } %>`

    The `handleFormEmails` import should be conditional on form-builder plugin being selected.

    SMTP adapter: Keep as-is (it's already runtime-conditional via `process.env.SMTP_HOST`).

    Remove ALL `// @setup:` comments — they are not needed in the template output.

    **templates/Posts.ts.ejs:**
    Base on current `src/collections/Posts.ts`. The categories and tags relationship fields are ALWAYS included because the prompt enforces that selecting Posts auto-includes Categories and Tags. So this template is actually identical to the current Posts.ts minus any @setup markers. Keep it as a template anyway for consistency and future flexibility.

    **templates/TemplateParts.ts.ejs:**
    Base on current `src/collections/TemplateParts.ts`. The `displayCondition.collectionType` options should be conditional:
    ```
    options: [
      { label: 'All Pages', value: 'pages' },
    <% if (collections.includes('posts')) { %>
      { label: 'All Blog Posts', value: 'posts' },
    <% } %>
    ],
    ```

    IMPORTANT for all templates:
    - Use `<%` and `%>` (not `<%=`) for control flow — we're generating source code, not HTML
    - Use `<%-` for any interpolated values to avoid HTML escaping
    - Ensure proper indentation in the output — the generated TypeScript should be well-formatted
    - Test mentally: with all features selected, output should match the current file (minus @setup comments). With features deselected, output should be valid TypeScript with those features cleanly removed.
  </action>
  <verify>
    <automated>cd C:/Projects/sandbox/create-payload-starter && node -e "const ejs = require('ejs'); const fs = require('fs'); const t = fs.readFileSync('templates/payload.config.ts.ejs','utf8'); const r = ejs.render(t, {collections:['posts','categories','tags','template-parts'],plugins:['seo','redirects','form-builder','nested-docs','import-export','mcp','search']}); if(!r.includes('Posts') || !r.includes('seoPlugin') || r.includes('@setup')) { process.exit(1); } console.log('Full selection OK'); const r2 = ejs.render(t, {collections:[],plugins:[]}); if(r2.includes('Posts') || r2.includes('seoPlugin')) { process.exit(1); } console.log('Empty selection OK');"</automated>
  </verify>
  <done>
    - payload.config.ts.ejs renders valid TypeScript for all-selected and none-selected scenarios
    - Posts.ts.ejs renders the Posts collection without @setup markers
    - TemplateParts.ts.ejs conditionally includes 'posts' in collectionType options
    - No @setup markers appear in any rendered output
    - Plugin collection arrays are correctly conditional (e.g., SEO with ['pages'] when posts deselected)
  </done>
</task>

</tasks>

<verification>
1. `cd C:/Projects/sandbox/create-payload-starter && npx tsc --noEmit` — TypeScript compiles
2. EJS templates render valid output for full-selection and minimal-selection scenarios
3. package.json has correct bin entry and all dependencies
4. Collection dependency enforcement in prompts (Posts -> Categories + Tags)
</verification>

<success_criteria>
- Complete CLI package at C:/Projects/sandbox/create-payload-starter/ that compiles
- Three EJS templates that produce clean TypeScript output
- Prompt logic enforces collection dependencies
- Scaffold, render, and post-scaffold modules ready for end-to-end testing
</success_criteria>

<output>
After completion, create `.planning/phases/06-developer-experience/06-04-SUMMARY.md`
</output>
