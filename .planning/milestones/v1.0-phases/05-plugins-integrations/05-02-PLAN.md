---
phase: 05-plugins-integrations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/customiser/index.tsx
  - src/views/customiser/index.client.tsx
  - src/views/customiser/index.scss
  - src/views/customiser/DocumentFields.tsx
  - src/views/customiser/DocumentFields.scss
  - src/views/customiser/context/SelectedSectionContext.tsx
  - src/views/customiser/SectionFields/index.tsx
  - src/views/customiser/SectionFields/index.scss
  - src/views/customiser/SectionFields/DragContext.tsx
  - src/views/customiser/SectionFields/InlineBlockAdder.tsx
  - src/views/customiser/SectionFields/InlineBlockAdder.scss
  - src/views/customiser/SectionFields/SectionRow.tsx
  - src/views/customiser/SectionFields/SectionRowButton.tsx
  - src/views/customiser/SectionFields/getFieldByPath.ts
  - src/views/customiser/LivePreview/Preview/index.tsx
  - src/views/customiser/LivePreview/Preview/index.scss
  - src/views/customiser/LivePreview/IFrame/index.tsx
  - src/views/customiser/LivePreview/IFrame/index.scss
  - src/views/customiser/LivePreview/Context/context.ts
  - src/views/customiser/LivePreview/Context/index.tsx
  - src/views/customiser/LivePreview/Context/sizeReducer.ts
  - src/views/customiser/LivePreview/Context/collisionDetection.ts
  - src/views/customiser/LivePreview/Device/index.tsx
  - src/views/customiser/LivePreview/DeviceContainer/index.tsx
  - src/views/customiser/LivePreview/Toolbar/index.tsx
  - src/views/customiser/LivePreview/Toolbar/index.scss
  - src/views/customiser/LivePreview/Toolbar/Controls/index.tsx
  - src/views/customiser/LivePreview/Toolbar/Controls/index.scss
  - src/views/customiser/LivePreview/Toolbar/SizeInput/index.tsx
  - src/views/customiser/LivePreview/Toolbar/SizeInput/index.scss
  - src/views/customiser/LivePreview/ToolbarArea/index.tsx
  - src/views/customiser/LivePreview/ToolbarArea/index.scss
  - src/views/customiser/LivePreview/usePopupWindow.ts
  - src/views/customiser/components/Tabs/index.tsx
  - src/views/customiser/components/Tabs/index.scss
  - src/views/customiser/components/BlocksTreeView/index.tsx
  - src/views/customiser/components/BlocksTreeView/index.scss
  - src/views/customiser/components/BlocksTreeView/ArrayAction.tsx
  - src/views/customiser/components/BlocksTreeView/BlockRow.tsx
  - src/views/customiser/components/BlocksTreeView/RowActions.tsx
  - src/views/customiser/components/BlocksTreeView/rowHelpers.ts
  - src/utilities/customiserConfig.ts
  - src/collections/Pages.ts
  - src/collections/Posts.ts
  - src/collections/TemplateParts.ts
autonomous: true
requirements: [INTG-01]

must_haves:
  truths:
    - "Customiser tab appears on Pages, Posts, and Template Parts edit views"
    - "Customiser shows 3-pane layout: block tree (left), preview iframe (center), field editor (right)"
    - "Block tree sidebar shows blocks from the 'layout' field (not 'sections')"
    - "Preview iframe loads the page's live preview URL using RefreshRouteOnSave (not postMessage)"
    - "Toolbar shows breakpoint/zoom/device controls"
    - "Popup window option works for detached preview"
    - "Drag-and-drop block reordering works in the tree sidebar"
    - "Selecting a block in the tree shows its fields in the right panel"
  artifacts:
    - path: "src/views/customiser/index.tsx"
      provides: "RSC entry point for customiser view"
      exports: ["CustomiserView"]
    - path: "src/views/customiser/index.client.tsx"
      provides: "Client-side customiser with 3-pane layout"
      exports: ["CustomiserClient"]
    - path: "src/utilities/customiserConfig.ts"
      provides: "Configurable collection slugs and blocks field name"
      exports: ["CUSTOMISER_COLLECTIONS", "CUSTOMISER_BLOCKS_FIELD"]
  key_links:
    - from: "src/collections/Pages.ts"
      to: "src/views/customiser/index.tsx"
      via: "admin.components.views.edit.customiser"
      pattern: "CustomiserView"
    - from: "src/views/customiser/LivePreview/Preview/index.tsx"
      to: "iframe"
      via: "RefreshRouteOnSave in frontend layout"
      pattern: "iframe.*url"
    - from: "src/views/customiser/index.client.tsx"
      to: "src/views/customiser/SectionFields/index.tsx"
      via: "block tree rendering"
      pattern: "SectionFields"
---

<objective>
Copy the Layout Customizer source from the external plugin into `src/views/customiser/` and adapt it for direct integration: change field name from 'sections' to configurable 'layout', replace postMessage-based live preview with RefreshRouteOnSave, register as a custom edit view tab on Pages/Posts/TemplateParts.

Purpose: Provides a Shopify-style visual editor for block-based layouts without requiring the external plugin package.
Output: Customiser tab on all layout collections with 3-pane editor (tree + preview + fields).
</objective>

<execution_context>
@C:/Users/Eugene/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eugene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-plugins-integrations/05-RESEARCH.md
@.planning/phases/05-plugins-integrations/05-CONTEXT.md

@src/payload.config.ts
@src/collections/Pages.ts
@src/collections/Posts.ts
@src/collections/TemplateParts.ts
@src/components/LivePreviewListener.tsx
@src/blocks/RenderBlocks.tsx

<interfaces>
<!-- Source customizer files to copy and adapt from -->
Source directory: C:\Projects\sandbox\payload\payload-customiser\src

Key adaptations required:
1. All 'sections' references -> configurable field name (default 'layout')
2. Plugin factory (index.ts payloadCustomiser()) -> direct view registration on collections
3. Import paths: 'payload-customiser/rsc/' -> '@/views/customiser/'
4. Import paths: '@/components/Tabs' -> '@/views/customiser/components/Tabs'
5. Import paths: '@/lib/utils' -> keep (same path exists in starter)
6. Preview component: remove useAllFormFields, reduceFieldsToValues, postMessage useEffects
7. Preview relies on RefreshRouteOnSave already in frontend layout

From src/collections/Pages.ts (view registration target):
```typescript
admin: {
  // Add components.views.edit.customiser here
  livePreview: { url: ({ data, req }) => generatePreviewPath({ ... }) },
}
fields: [
  { name: 'layout', type: 'blocks', blockReferences: [...allBlockSlugs] as BlockSlug[], blocks: [] },
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy and adapt customiser source files</name>
  <files>
    src/views/customiser/index.tsx,
    src/views/customiser/index.client.tsx,
    src/views/customiser/index.scss,
    src/views/customiser/DocumentFields.tsx,
    src/views/customiser/DocumentFields.scss,
    src/views/customiser/context/SelectedSectionContext.tsx,
    src/views/customiser/SectionFields/index.tsx,
    src/views/customiser/SectionFields/index.scss,
    src/views/customiser/SectionFields/DragContext.tsx,
    src/views/customiser/SectionFields/InlineBlockAdder.tsx,
    src/views/customiser/SectionFields/InlineBlockAdder.scss,
    src/views/customiser/SectionFields/SectionRow.tsx,
    src/views/customiser/SectionFields/SectionRowButton.tsx,
    src/views/customiser/SectionFields/getFieldByPath.ts,
    src/views/customiser/LivePreview/Preview/index.tsx,
    src/views/customiser/LivePreview/Preview/index.scss,
    src/views/customiser/LivePreview/IFrame/index.tsx,
    src/views/customiser/LivePreview/IFrame/index.scss,
    src/views/customiser/LivePreview/Context/context.ts,
    src/views/customiser/LivePreview/Context/index.tsx,
    src/views/customiser/LivePreview/Context/sizeReducer.ts,
    src/views/customiser/LivePreview/Context/collisionDetection.ts,
    src/views/customiser/LivePreview/Device/index.tsx,
    src/views/customiser/LivePreview/DeviceContainer/index.tsx,
    src/views/customiser/LivePreview/Toolbar/index.tsx,
    src/views/customiser/LivePreview/Toolbar/index.scss,
    src/views/customiser/LivePreview/Toolbar/Controls/index.tsx,
    src/views/customiser/LivePreview/Toolbar/Controls/index.scss,
    src/views/customiser/LivePreview/Toolbar/SizeInput/index.tsx,
    src/views/customiser/LivePreview/Toolbar/SizeInput/index.scss,
    src/views/customiser/LivePreview/ToolbarArea/index.tsx,
    src/views/customiser/LivePreview/ToolbarArea/index.scss,
    src/views/customiser/LivePreview/usePopupWindow.ts,
    src/views/customiser/components/Tabs/index.tsx,
    src/views/customiser/components/Tabs/index.scss,
    src/views/customiser/components/BlocksTreeView/index.tsx,
    src/views/customiser/components/BlocksTreeView/index.scss,
    src/views/customiser/components/BlocksTreeView/ArrayAction.tsx,
    src/views/customiser/components/BlocksTreeView/BlockRow.tsx,
    src/views/customiser/components/BlocksTreeView/RowActions.tsx,
    src/views/customiser/components/BlocksTreeView/rowHelpers.ts,
    src/utilities/customiserConfig.ts
  </files>
  <action>
1. Create `src/utilities/customiserConfig.ts` with the configurable settings:
```typescript
export const CUSTOMISER_COLLECTIONS = ['pages', 'posts', 'template-parts'] as const
export const CUSTOMISER_BLOCKS_FIELD = 'layout'
```

2. Copy ALL files from `C:\Projects\sandbox\payload\payload-customiser\src\views\Customiser\` into `src/views/customiser/` (lowercase directory name). Also copy `components/BlocksTreeView/` and `components/Tabs/` into `src/views/customiser/components/`.

3. Apply these adaptations across ALL copied files:

**Import path fixes (apply to every file):**
- `'payload-customiser/rsc/#CustomiserView'` -> `'@/views/customiser/index#CustomiserView'`
- Any `'@/components/Tabs'` imports -> `'@/views/customiser/components/Tabs'`
- Any `'@/components/BlocksTreeView'` imports -> `'@/views/customiser/components/BlocksTreeView'`
- Any `'@/hooks/use-mobile'` -> inline or remove (check if needed)
- Verify `'@/lib/utils'` import for `cn()` stays the same (already exists in starter)

**Field name: 'sections' -> configurable 'layout' (critical -- see pitfall 2 in RESEARCH.md):**
- `src/views/customiser/DocumentFields.tsx` line 267: `field.name === 'sections'` -> `field.name === CUSTOMISER_BLOCKS_FIELD` (import from customiserConfig)
- `src/views/customiser/DocumentFields.tsx` line 269: `field.name !== 'sections'` -> `field.name !== CUSTOMISER_BLOCKS_FIELD`
- `src/views/customiser/index.client.tsx` line 379: `field.name === 'sections'` -> `field.name === CUSTOMISER_BLOCKS_FIELD`
- Tab label "Section" -> "Layout" in DocumentFields.tsx TabsTrigger
- Tab value "sections" -> "layout" in DocumentFields.tsx Tabs/TabsContent
- All `sectionsField` variable names -> `layoutField` for clarity
- `hasSectionFields` -> `hasLayoutFields`
- `SectionFields` component name can stay (it's the tree sidebar, naming is fine)

**Preview component rewrite (`src/views/customiser/LivePreview/Preview/index.tsx`):**
Remove the postMessage-based live preview code. The preview iframe just loads the URL -- RefreshRouteOnSave in the frontend layout handles auto-refresh on save. Specifically:
- REMOVE: `import { useAllFormFields } from '@payloadcms/ui'`
- REMOVE: `import { reduceFieldsToValues } from 'payload/shared'`
- REMOVE: The first `useEffect` that sends `postMessage` with `payload-live-preview` type and form field values
- REMOVE: The second `useEffect` that sends `postMessage` with `payload-document-event` type
- REMOVE: `const [fields] = useAllFormFields()`
- REMOVE: `fieldSchemaJSON` references
- KEEP: The iframe rendering with DeviceContainer, Toolbar, url handling, popup window support
- KEEP: `useLivePreviewContext()` for breakpoints, iframe refs, popup state

The simplified Preview component should just render the iframe pointed at the URL. The auto-save (300ms) triggers database save, then RefreshRouteOnSave in the iframe's frontend layout calls router.refresh() to update.

**RSC view entry point (`src/views/customiser/index.tsx`):**
Keep mostly as-is from the plugin source. It already extracts breakpoints and URL from livePreviewConfig. Just update the import path for CustomiserClient:
```typescript
import { CustomiserClient } from './index.client'
```

4. Check if `@dnd-kit/core` and `@dnd-kit/sortable` are available from Payload's dependencies. Run:
```bash
ls node_modules/@dnd-kit/core 2>/dev/null && echo "EXISTS" || echo "MISSING"
ls node_modules/@dnd-kit/sortable 2>/dev/null && echo "EXISTS" || echo "MISSING"
```
If MISSING, install them: `pnpm add @dnd-kit/core @dnd-kit/sortable`

5. Do NOT copy the plugin's `index.ts` (the `payloadCustomiser` factory function) or `exports/` directory -- those are the plugin packaging layer we're replacing.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>All customiser source files copied and adapted into src/views/customiser/. Field name changed from 'sections' to 'layout'. PostMessage preview replaced with simple iframe (relies on RefreshRouteOnSave). Import paths fixed. dnd-kit dependencies verified.</done>
</task>

<task type="auto">
  <name>Task 2: Register customiser view on collections</name>
  <files>src/collections/Pages.ts, src/collections/Posts.ts, src/collections/TemplateParts.ts</files>
  <action>
Add the customiser view tab to all 3 collection configs. In each collection's `admin` block, add `components.views.edit.customiser`:

```typescript
admin: {
  // ...existing config (defaultColumns, useAsTitle, livePreview, preview)
  components: {
    views: {
      edit: {
        customiser: {
          Component: '@/views/customiser/index#CustomiserView',
          path: '/customiser',
          tab: {
            href: '/customiser',
            label: 'Customiser',
          },
        },
      },
    },
  },
},
```

Apply this to:
1. `src/collections/Pages.ts` - add `components` to existing `admin` object
2. `src/collections/Posts.ts` - add `components` to existing `admin` object
3. `src/collections/TemplateParts.ts` - add `components` to existing `admin` object

The existing admin properties (defaultColumns, useAsTitle, livePreview, preview) must be preserved. Only add the `components` property.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Customiser tab registered on Pages, Posts, and Template Parts. Navigating to /admin/collections/pages/{id}/customiser shows the 3-pane customiser layout.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- All customiser files exist in src/views/customiser/
- No references to 'sections' field name remain (should be 'layout')
- No postMessage preview code remains in Preview component
- All 3 collection configs have customiser view tab
</verification>

<success_criteria>
- Customiser tab appears on Pages, Posts, and Template Parts edit views
- 3-pane layout renders (block tree + preview + fields)
- Preview uses RefreshRouteOnSave (no postMessage)
- Block tree reads from 'layout' field
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-plugins-integrations/05-02-SUMMARY.md`
</output>
