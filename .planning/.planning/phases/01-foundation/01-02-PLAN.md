---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/(frontend)/globals.css
  - src/app/(frontend)/layout.tsx
  - src/app/(frontend)/page.tsx
  - src/lib/utils.ts
  - components.json
  - src/components/ui/*
autonomous: false
requirements:
  - DX-06

must_haves:
  truths:
    - "Tailwind v4 CSS-first configuration works with @theme directive"
    - "All shadcn/ui components are installed and available"
    - "Frontend layout uses RSC architecture -- client components only at leaf nodes"
    - "shadcn/ui styles do not leak into Payload admin panel"
    - "A shadcn/ui Button renders correctly on the frontend homepage"
  artifacts:
    - path: "src/app/(frontend)/globals.css"
      provides: "Tailwind v4 CSS imports and shadcn theme variables"
      contains: "@import \"tailwindcss\""
    - path: "src/lib/utils.ts"
      provides: "cn() utility for class merging"
      exports: ["cn"]
    - path: "components.json"
      provides: "shadcn/ui configuration"
      contains: "\"style\": \"default\""
    - path: "src/app/(frontend)/layout.tsx"
      provides: "Frontend root layout with Tailwind styles scoped to frontend"
  key_links:
    - from: "src/app/(frontend)/layout.tsx"
      to: "src/app/(frontend)/globals.css"
      via: "CSS import"
      pattern: "import.*globals\\.css"
    - from: "src/app/(frontend)/page.tsx"
      to: "src/components/ui/button"
      via: "shadcn Button import"
      pattern: "import.*Button.*from"
---

<objective>
Set up the frontend stack with Tailwind v4 CSS-first configuration, install all shadcn/ui components, configure the frontend layout with proper RSC architecture, run initial migration, and verify the complete foundation builds and runs.

Purpose: Completes the frontend tooling so Phase 2 can immediately build block components with Tailwind classes and shadcn/ui primitives.
Output: Working frontend with Tailwind v4 + all shadcn/ui components, verified build, admin panel accessible.
</objective>

<execution_context>
@C:/Users/Eugene/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eugene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn/ui, install all components, configure Tailwind v4 frontend</name>
  <files>src/app/(frontend)/globals.css, src/app/(frontend)/layout.tsx, src/app/(frontend)/page.tsx, src/lib/utils.ts, components.json, src/components/ui/*</files>
  <action>
1. Initialize shadcn/ui with defaults (zinc theme):
   ```
   pnpx shadcn@latest init --defaults
   ```
   This creates `components.json`, `src/lib/utils.ts` (cn utility), and updates CSS.

   If shadcn asks about the CSS file location, point it to `src/app/(frontend)/globals.css` -- NOT a global CSS that would affect the admin panel.

2. Install ALL shadcn/ui components (user locked "install ALL shadcn/ui components upfront"):
   ```
   pnpx shadcn@latest add --all
   ```
   This installs every component into `src/components/ui/`.

3. Verify/update `src/app/(frontend)/globals.css` to use Tailwind v4 CSS-first config:
   ```css
   @import "tailwindcss";
   @import "tw-animate-css";

   @custom-variant dark (&:is(.dark *));

   @theme inline {
     /* shadcn/ui zinc theme CSS variables -- these should be generated by shadcn init */
     --color-background: oklch(1 0 0);
     --color-foreground: oklch(0.145 0 0);
     /* ... rest of shadcn theme variables ... */
     --radius: 0.625rem;
   }
   ```
   Do NOT use a tailwind.config.js -- Tailwind v4 is CSS-first.

4. Configure `src/app/(frontend)/layout.tsx` as a proper RSC layout:
   - Import `globals.css` (scoped to frontend only -- this prevents CSS leaking into admin)
   - Import Geist font
   - Set metadata (title: "Payload Starter", description)
   - Wrap children in a semantic HTML structure (html, body)
   - This is a Server Component -- no 'use client'

5. Update `src/app/(frontend)/page.tsx` to be a minimal homepage that proves shadcn/ui works:
   - Import and render a shadcn Button component
   - Simple heading "Payload Starter" and a styled Button
   - This is a Server Component

6. Verify `src/lib/utils.ts` has the cn() utility:
   ```typescript
   import { clsx, type ClassValue } from 'clsx'
   import { twMerge } from 'tailwind-merge'

   export function cn(...inputs: ClassValue[]) {
     return twMerge(clsx(inputs))
   }
   ```

7. IMPORTANT: Ensure CSS isolation -- the `globals.css` import MUST be in `(frontend)/layout.tsx` only, NOT in any layout that serves the `(payload)` admin routes. The admin panel has its own styles.
  </action>
  <verify>
    <automated>cd C:/Projects/sandbox/payload-starter && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>shadcn/ui initialized with zinc theme. All components installed in src/components/ui/. Tailwind v4 CSS-first config in globals.css with @theme directive. Frontend layout imports globals.css (scoped, no admin leak). cn() utility available. Homepage renders shadcn Button.</done>
</task>

<task type="auto">
  <name>Task 2: Run initial migration, generate types, verify full build</name>
  <files>src/payload-types.ts, src/migrations/*</files>
  <action>
1. Ensure PostgreSQL is accessible at the DATABASE_URL in .env. If the database doesn't exist, create it:
   ```
   psql -U postgres -c "CREATE DATABASE payload_starter;" 2>/dev/null || echo "Database may already exist"
   ```

2. Create the initial migration:
   ```
   pnpm payload migrate:create
   ```
   This generates migration files based on the current Payload config (Users, Media collections with blocksAsJSON).

3. Run the migration:
   ```
   pnpm payload migrate
   ```

4. Generate TypeScript types:
   ```
   pnpm payload generate:types
   ```
   This creates `src/payload-types.ts` based on the config with interfaceName conventions.

5. Run a fast type-check first, then the full build:
   ```
   npx tsc --noEmit && pnpm build
   ```
   The type-check catches errors quickly before the slower build. Both must succeed. If there are errors, fix them before proceeding.

6. After successful build, verify key files exist and contain expected content:
   - `src/payload-types.ts` exists and was generated
   - `src/payload.config.ts` contains `blocksAsJSON: true`
   - `src/blocks/registry.ts` exports allBlocks
   - `src/components/ui/button.tsx` exists (shadcn installed)
   - No `src/heros/` directory
   - No template block directories in `src/blocks/`
  </action>
  <verify>
    <automated>cd C:/Projects/sandbox/payload-starter && npx tsc --noEmit 2>&1 | head -20 && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>Initial migration created and run successfully. TypeScript types generated. Type-check and full build pass. Database has blocksAsJSON storage (JSONB columns, not relational block tables). All Phase 1 artifacts verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify foundation -- admin panel, Lexical editor, shadcn/ui rendering</name>
  <files>none -- verification only</files>
  <action>
Human verifies the complete Phase 1 foundation by running the dev server and checking:
1. Admin panel loads and is functional
2. Lexical rich text editor works
3. Frontend renders with shadcn/ui styling
4. No CSS leakage between frontend and admin
  </action>
  <verify>
    <automated>cd C:/Projects/sandbox/payload-starter && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>User has verified: admin panel loads at /admin, Lexical editor works, frontend shows styled shadcn Button, admin panel styles are not broken by Tailwind.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes without errors
- Admin panel loads at /admin
- Frontend renders at / with shadcn/ui Button
- Tailwind v4 CSS-first config (no tailwind.config.js)
- shadcn/ui components present in src/components/ui/
- No template blocks remain
- blocksAsJSON: true in payload.config.ts
- Block registry exports allBlocks array
</verification>

<success_criteria>
- All shadcn/ui components installed and rendering with zinc theme
- Tailwind v4 CSS-first configuration working
- Frontend CSS scoped to (frontend) route group only
- Initial database migration created and run
- TypeScript types generated
- Full build passes
- Admin panel accessible and functional with Lexical editor
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
