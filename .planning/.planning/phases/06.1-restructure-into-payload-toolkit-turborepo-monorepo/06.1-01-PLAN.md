---
phase: 06.1-restructure-into-payload-toolkit-turborepo-monorepo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Projects/sandbox/payload-toolkit/package.json
  - C:/Projects/sandbox/payload-toolkit/pnpm-workspace.yaml
  - C:/Projects/sandbox/payload-toolkit/turbo.json
  - C:/Projects/sandbox/payload-toolkit/.gitignore
  - C:/Projects/sandbox/payload-toolkit/packages/shared/package.json
  - C:/Projects/sandbox/payload-toolkit/packages/shared/tsconfig.json
  - C:/Projects/sandbox/payload-toolkit/packages/shared/src/index.ts
  - C:/Projects/sandbox/payload-toolkit/packages/shared/src/create-database.ts
  - C:/Projects/sandbox/payload-toolkit/packages/shared/src/generate-env.ts
  - C:/Projects/sandbox/payload-toolkit/apps/starter/**
autonomous: true
requirements: []

must_haves:
  truths:
    - "Monorepo root exists at C:/Projects/sandbox/payload-toolkit/ with Turborepo + pnpm workspaces configured"
    - "Shared package at packages/shared/ exports createDatabase and generateEnv"
    - "Starter app at apps/starter/ contains all source from payload-starter repo (minus .planning/)"
    - "pnpm install succeeds at root level and resolves workspace packages"
  artifacts:
    - path: "package.json"
      provides: "Root monorepo config (private, turbo scripts, packageManager)"
    - path: "pnpm-workspace.yaml"
      provides: "Workspace package definitions"
    - path: "turbo.json"
      provides: "Task pipeline configuration"
    - path: "packages/shared/src/index.ts"
      provides: "Shared utility barrel exports"
      exports: ["createDatabase", "generateEnv"]
    - path: "apps/starter/package.json"
      provides: "Starter app package config (name: payload-starter)"
  key_links:
    - from: "packages/shared/package.json"
      to: "pnpm-workspace.yaml"
      via: "workspace package resolution"
    - from: "apps/starter/package.json"
      to: "pnpm-workspace.yaml"
      via: "workspace package resolution"
---

<objective>
Create the payload-toolkit monorepo scaffold at C:/Projects/sandbox/payload-toolkit/ with Turborepo + pnpm workspaces, the shared utility package, and copy the starter app.

Purpose: Establishes the monorepo foundation that Plan 02 (CLI migration) depends on.
Output: A monorepo with root config, packages/shared/ built and exporting utilities, and apps/starter/ containing the full starter app.
</objective>

<execution_context>
@C:/Users/Eugene/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eugene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Projects/sandbox/payload-starter/.planning/phases/06.1-restructure-into-payload-toolkit-turborepo-monorepo/06.1-CONTEXT.md
@C:/Projects/sandbox/payload-starter/.planning/phases/06.1-restructure-into-payload-toolkit-turborepo-monorepo/06.1-RESEARCH.md

<interfaces>
<!-- Shared package contracts that Plan 02 (CLI) will import -->

From packages/shared/src/create-database.ts:
```typescript
export type DatabaseConfig = {
  host: string
  port: number
  user: string
  password: string
  database: string
}
export function createDatabase(config: DatabaseConfig): Promise<void>
```

From packages/shared/src/generate-env.ts:
```typescript
export type GenerateEnvOptions = {
  targetDir: string
  projectName: string
  dbHost: string
  dbPort: string
  dbUser: string
  dbPassword: string
  adminEmail?: string
  overwrite?: boolean
  smtp?: {
    host: string
    port: string
    user: string
    pass: string
    from: string
  }
}
export function generateEnv(options: GenerateEnvOptions): void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo root and shared package</name>
  <files>
    C:/Projects/sandbox/payload-toolkit/package.json,
    C:/Projects/sandbox/payload-toolkit/pnpm-workspace.yaml,
    C:/Projects/sandbox/payload-toolkit/turbo.json,
    C:/Projects/sandbox/payload-toolkit/.gitignore,
    C:/Projects/sandbox/payload-toolkit/packages/shared/package.json,
    C:/Projects/sandbox/payload-toolkit/packages/shared/tsconfig.json,
    C:/Projects/sandbox/payload-toolkit/packages/shared/src/index.ts,
    C:/Projects/sandbox/payload-toolkit/packages/shared/src/create-database.ts,
    C:/Projects/sandbox/payload-toolkit/packages/shared/src/generate-env.ts
  </files>
  <action>
Create directory C:/Projects/sandbox/payload-toolkit/ and the following files:

1. **Root package.json** -- private monorepo root:
   ```json
   {
     "name": "payload-toolkit",
     "private": true,
     "scripts": {
       "build": "turbo run build",
       "dev": "turbo run dev",
       "lint": "turbo run lint",
       "typecheck": "turbo run typecheck"
     },
     "devDependencies": {
       "turbo": "^2.0.0"
     },
     "packageManager": "pnpm@10.0.0"
   }
   ```
   Check the user's actual pnpm version with `pnpm --version` and use that in packageManager.

2. **pnpm-workspace.yaml**:
   ```yaml
   packages:
     - "apps/*"
     - "packages/*"
   ```

3. **turbo.json** -- task pipeline:
   ```json
   {
     "$schema": "https://turbo.build/schema.json",
     "tasks": {
       "build": {
         "dependsOn": ["^build"],
         "outputs": ["dist/**", ".next/**"]
       },
       "dev": {
         "persistent": true,
         "cache": false
       },
       "lint": {},
       "typecheck": {
         "dependsOn": ["^build"]
       }
     }
   }
   ```

4. **Root .gitignore** -- standard monorepo ignores: node_modules, dist, .next, .turbo, .env, .env.local, *.tsbuildinfo. Do NOT include .planning/ in ignore (it stays in monorepo root for dev use).

5. **packages/shared/package.json**:
   ```json
   {
     "name": "@jon8800/shared",
     "version": "0.1.0",
     "type": "module",
     "main": "./dist/index.js",
     "types": "./dist/index.d.ts",
     "exports": {
       ".": {
         "types": "./dist/index.d.ts",
         "default": "./dist/index.js"
       }
     },
     "scripts": {
       "build": "tsc",
       "typecheck": "tsc --noEmit"
     },
     "dependencies": {
       "pg": "^8.20.0"
     },
     "devDependencies": {
       "@types/pg": "^8.18.0",
       "@types/node": "^22.0.0",
       "typescript": "^5.7.0"
     }
   }
   ```

6. **packages/shared/tsconfig.json**: target ES2022, module Node16, moduleResolution Node16, outDir ./dist, rootDir ./src, strict true, declaration true, skipLibCheck true.

7. **packages/shared/src/create-database.ts**: Copy from C:/Projects/sandbox/create-payload-starter/src/lib/create-database.ts. Export the DatabaseConfig type as well (`export type DatabaseConfig = ...`).

8. **packages/shared/src/generate-env.ts**: Copy from C:/Projects/sandbox/create-payload-starter/src/lib/generate-env.ts (the version that takes `targetDir` parameter). Export the GenerateEnvOptions type as well (`export type GenerateEnvOptions = ...`).

9. **packages/shared/src/index.ts**: Barrel file re-exporting everything:
   ```typescript
   export { createDatabase } from './create-database.js'
   export type { DatabaseConfig } from './create-database.js'
   export { generateEnv } from './generate-env.js'
   export type { GenerateEnvOptions } from './generate-env.js'
   ```

After creating files, run `git init` in the monorepo root with `main` as default branch: `git init -b main`.
  </action>
  <verify>
    <automated>cd C:/Projects/sandbox/payload-toolkit && ls package.json pnpm-workspace.yaml turbo.json .gitignore packages/shared/package.json packages/shared/src/index.ts</automated>
  </verify>
  <done>Monorepo root exists with all config files. Shared package has source files with exported types and functions.</done>
</task>

<task type="auto">
  <name>Task 2: Copy starter app into apps/starter/ and install</name>
  <files>C:/Projects/sandbox/payload-toolkit/apps/starter/**</files>
  <action>
1. Create `C:/Projects/sandbox/payload-toolkit/apps/starter/` directory.

2. Copy the ENTIRE contents of `C:/Projects/sandbox/payload-starter/` into `C:/Projects/sandbox/payload-toolkit/apps/starter/`, EXCLUDING:
   - `.planning/` directory (dev-only, stays at monorepo root or separate)
   - `.git/` directory
   - `node_modules/` directory
   - `.next/` directory
   - `.env` file (secrets)

   Use a recursive copy command: `cp -r` with exclusions, or xcopy. On Windows/MSYS, use:
   ```bash
   rsync -av --exclude='.planning' --exclude='.git' --exclude='node_modules' --exclude='.next' --exclude='.env' C:/Projects/sandbox/payload-starter/ C:/Projects/sandbox/payload-toolkit/apps/starter/
   ```
   If rsync is not available, use `cp -r` and then delete the excluded dirs.

3. The starter's package.json name should remain `"payload-starter"` (NOT scoped -- end users get this as their project).

4. Also copy the monorepo's `.planning/` directory to the monorepo root (NOT into apps/starter/):
   ```bash
   cp -r C:/Projects/sandbox/payload-starter/.planning C:/Projects/sandbox/payload-toolkit/.planning
   ```

5. Add `pnpm.onlyBuiltDependencies` to the ROOT package.json to support workspace-wide sharp/esbuild:
   ```json
   "pnpm": {
     "onlyBuiltDependencies": ["sharp", "esbuild", "unrs-resolver"]
   }
   ```

6. Run `pnpm install` at the monorepo root to install all workspace dependencies and link packages.

7. Verify shared package builds: `cd packages/shared && pnpm build`
  </action>
  <verify>
    <automated>cd C:/Projects/sandbox/payload-toolkit && ls apps/starter/package.json apps/starter/src/payload.config.ts packages/shared/dist/index.js && pnpm ls --filter @jon8800/shared</automated>
  </verify>
  <done>Starter app is in apps/starter/ with all source files. pnpm install succeeded. Shared package built to dist/. No .planning/ or .git/ in apps/starter/.</done>
</task>

</tasks>

<verification>
- `C:/Projects/sandbox/payload-toolkit/` exists with root package.json, pnpm-workspace.yaml, turbo.json
- `pnpm install` at root resolves all packages without errors
- `packages/shared/dist/index.js` exists (shared package builds)
- `apps/starter/package.json` exists with name "payload-starter"
- No `.planning/`, `.git/`, `node_modules/` in `apps/starter/`
</verification>

<success_criteria>
- Monorepo root configured with Turborepo + pnpm workspaces
- Shared package builds and exports createDatabase + generateEnv
- Starter app fully copied into apps/starter/ with all source intact
- pnpm workspace links resolve correctly
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-restructure-into-payload-toolkit-turborepo-monorepo/06.1-01-SUMMARY.md`
</output>
