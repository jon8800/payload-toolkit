---
phase: 06.1-restructure-into-payload-toolkit-turborepo-monorepo
plan: 03
type: execute
wave: 3
depends_on: ["06.1-02"]
files_modified:
  - C:/Projects/sandbox/payload-toolkit/README.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "turbo build succeeds across all workspace packages (shared, CLI, starter)"
    - "turbo typecheck passes for shared and CLI packages"
    - "GitHub repo jon8800/payload-toolkit exists and has the monorepo code pushed"
    - "Old repos jon8800/payload-starter and jon8800/create-payload-starter are archived"
  artifacts:
    - path: "packages/shared/dist/index.js"
      provides: "Built shared package"
    - path: "packages/create-payload-starter/dist/index.js"
      provides: "Built CLI entry point"
    - path: "apps/starter/package.json"
      provides: "Starter app config"
  key_links:
    - from: "packages/create-payload-starter/dist/scaffold.js"
      to: "https://codeload.github.com/jon8800/payload-toolkit/tar.gz/main"
      via: "tarball download URL"
---

<objective>
Verify the full monorepo builds, create the GitHub repo, push code, and archive old repos.

Purpose: Finalizes the monorepo migration. After this, payload-toolkit is the single source of truth.
Output: Working monorepo on GitHub, old repos archived.
</objective>

<execution_context>
@C:/Users/Eugene/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eugene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Projects/sandbox/payload-starter/.planning/phases/06.1-restructure-into-payload-toolkit-turborepo-monorepo/06.1-CONTEXT.md
@C:/Projects/sandbox/payload-starter/.planning/phases/06.1-restructure-into-payload-toolkit-turborepo-monorepo/06.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build verification, README, and GitHub repo creation</name>
  <files>C:/Projects/sandbox/payload-toolkit/README.md</files>
  <action>
1. Run full build from monorepo root:
   ```bash
   cd C:/Projects/sandbox/payload-toolkit && pnpm turbo build
   ```
   This should build: shared -> CLI (via ^build dependency). The starter app build is optional here (it requires DATABASE_URL env var at build time due to Payload 3.x). If starter build fails due to missing env, that is expected and acceptable -- the starter builds when end users set up their project.

2. Run typecheck for shared and CLI:
   ```bash
   cd C:/Projects/sandbox/payload-toolkit && pnpm turbo typecheck --filter=@jon8800/shared --filter=@jon8800/create-payload-starter
   ```

3. Verify workspace links:
   ```bash
   cd C:/Projects/sandbox/payload-toolkit && pnpm ls -r --depth 0
   ```

4. Create a minimal **README.md** at the monorepo root with: project name (payload-toolkit), description, package listing (apps/starter, packages/create-payload-starter, packages/shared), quick start command (`npx @jon8800/create-payload-starter`), and development instructions (`pnpm install && pnpm turbo build`).

5. Stage all files and create initial commit:
   ```bash
   cd C:/Projects/sandbox/payload-toolkit
   git add -A
   git commit -m "Initial monorepo structure with starter, CLI, and shared packages"
   ```

6. Create the GitHub repo and push:
   ```bash
   cd C:/Projects/sandbox/payload-toolkit
   gh repo create jon8800/payload-toolkit --public --source=. --push
   ```
  </action>
  <verify>
    <automated>cd C:/Projects/sandbox/payload-toolkit && pnpm turbo typecheck --filter=@jon8800/shared --filter=@jon8800/create-payload-starter</automated>
  </verify>
  <done>Full typecheck passes for shared and CLI. Code committed to git. GitHub repo created and code pushed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify GitHub repo and archive old repos</name>
  <files>N/A</files>
  <action>
This is a human verification checkpoint. Claude has automated everything above. The user now needs to verify the GitHub repo looks correct and archive the old repos.
  </action>
  <verify>User confirms GitHub repo structure is correct and old repos are archived.</verify>
  <done>GitHub repo jon8800/payload-toolkit verified. Old repos archived.</done>
  <what-built>
    The payload-toolkit monorepo has been created at C:/Projects/sandbox/payload-toolkit/ with:
    - apps/starter/ containing the full Payload CMS starter
    - packages/create-payload-starter/ with CLI using tarball extraction
    - packages/shared/ with shared database/env utilities
    - Pushed to GitHub at https://github.com/jon8800/payload-toolkit
  </what-built>
  <how-to-verify>
    1. Visit https://github.com/jon8800/payload-toolkit and confirm the repo exists with the correct structure
    2. Verify the directory structure shows apps/, packages/, turbo.json, pnpm-workspace.yaml
    3. If everything looks good, archive the old repos:
       - Go to https://github.com/jon8800/payload-starter/settings > Danger Zone > Archive this repository
       - Go to https://github.com/jon8800/create-payload-starter/settings > Danger Zone > Archive this repository
    4. Optionally test the CLI end-to-end: `npx @jon8800/create-payload-starter` (note: CLI package needs to be published to npm first for this to work via npx)
  </how-to-verify>
  <resume-signal>Type "approved" after verifying GitHub repo and archiving old repos, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- turbo typecheck passes for shared and CLI packages
- GitHub repo jon8800/payload-toolkit exists and is public
- Monorepo structure is correct (apps/starter, packages/create-payload-starter, packages/shared)
- Old repos archived (user manual step)
</verification>

<success_criteria>
- All workspace packages typecheck successfully
- Code pushed to jon8800/payload-toolkit on GitHub
- User confirms repo structure looks correct
- Old repos archived (or user plans to archive them)
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-restructure-into-payload-toolkit-turborepo-monorepo/06.1-03-SUMMARY.md`
</output>
