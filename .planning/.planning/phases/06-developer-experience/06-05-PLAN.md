---
phase: 06-developer-experience
plan: 05
type: execute
wave: 2
depends_on: [06-04]
files_modified:
  - src/payload.config.ts
  - scripts/setup.ts
  - scripts/lib/modify-config.ts
  - package.json
autonomous: true
gap_closure: true
requirements: [DX-01]

must_haves:
  truths:
    - "payload.config.ts has no @setup markers — it is clean source code with all features active"
    - "scripts/lib/modify-config.ts is deleted — comment-toggling approach is removed"
    - "scripts/setup.ts is either deleted or replaced with a minimal wrapper pointing to the CLI package"
    - "The starter repo builds cleanly after cleanup"
  artifacts:
    - path: "src/payload.config.ts"
      provides: "Clean payload config with all features, no @setup markers"
    - path: "package.json"
      provides: "Updated scripts without setup reference"
  key_links:
    - from: "src/payload.config.ts"
      to: "all collection and plugin imports"
      via: "direct imports without markers"
      pattern: "import.*from.*collections"
---

<objective>
Clean up the starter repo by removing @setup markers from payload.config.ts, deleting the old comment-toggling modify-config.ts, and updating/removing the in-place setup.ts script. The starter repo should be a clean, valid, buildable project with all features active — the CLI package handles feature selection via EJS templates.

Purpose: The starter repo is the template source. It must contain clean source code (no setup markers) since the CLI package's EJS templates now handle conditional generation. Leaving @setup markers would be confusing and the old modify-config.ts is dead code.

Output: A clean starter repo that builds, with no trace of the old comment-toggling setup approach.
</objective>

<execution_context>
@C:/Users/Eugene/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eugene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-developer-experience/06-CONTEXT.md
@.planning/phases/06-developer-experience/06-UAT.md
@.planning/phases/06-developer-experience/06-01-SUMMARY.md
@.planning/phases/06-developer-experience/06-04-SUMMARY.md

<interfaces>
<!-- Current state of files to be modified -->

From src/payload.config.ts:
- Lines with `// @setup:collection:X` markers on imports (lines 21-24) and array entries (lines 78-81)
- Lines with `// @setup:plugin:X` markers on imports (lines 5-11)
- Lines with `// @setup:plugin:X:start/end` block markers around plugin configs (lines 88-208)
- Lines with `// @setup:required` on Users, Media, Pages (lines 75-77)
- All markers should be removed, leaving clean import/array lines

From scripts/setup.ts:
- Interactive setup script that calls modifyConfig(), generateEnv(), createDatabase()
- This is being replaced by the CLI package

From scripts/lib/modify-config.ts:
- Comment-toggling logic — entirely replaced by EJS templates
- Should be deleted

From package.json scripts:
- "setup": "tsx scripts/setup.ts" — should be removed or updated
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove @setup markers from payload.config.ts and delete obsolete files</name>
  <files>
    src/payload.config.ts
    scripts/setup.ts
    scripts/lib/modify-config.ts
    package.json
  </files>
  <action>
    **1. Clean payload.config.ts:**
    Remove ALL `// @setup:*` comments from the file. This means:
    - Remove `// @setup:plugin:nested-docs` from the nestedDocsPlugin import line (keep the import)
    - Remove `// @setup:plugin:seo` from the seoPlugin import line (keep the import)
    - Remove `// @setup:plugin:redirects` from the redirectsPlugin import line
    - Remove `// @setup:plugin:form-builder` from the formBuilderPlugin import line
    - Remove `// @setup:plugin:import-export` from the importExportPlugin import line
    - Remove `// @setup:plugin:mcp` from the mcpPlugin import line
    - Remove `// @setup:plugin:search` from the searchPlugin import line
    - Remove `// @setup:collection:posts` from the Posts import AND array entry
    - Remove `// @setup:collection:categories` from the Categories import AND array entry
    - Remove `// @setup:collection:tags` from the Tags import AND array entry
    - Remove `// @setup:collection:template-parts` from the TemplateParts import AND array entry
    - Remove `// @setup:required` from Users, Media, Pages array entries
    - Remove `// @setup:plugin:X:start` and `// @setup:plugin:X:end` comment lines entirely (these are standalone comment lines wrapping plugin blocks — delete the comment lines, keep the plugin code)
    - The resulting file should be valid TypeScript with all features active and no setup-related comments

    **2. Delete scripts/lib/modify-config.ts:**
    This file is entirely replaced by EJS templates. Delete it.

    **3. Update scripts/setup.ts:**
    Replace the current setup.ts content with a minimal message pointing users to the CLI package:
    ```typescript
    console.log('')
    console.log('The setup script has moved to a separate CLI package.')
    console.log('Use: npx @yourscope/create-payload-starter')
    console.log('')
    console.log('See: https://github.com/yourscope/create-payload-starter')
    console.log('')
    process.exit(0)
    ```
    This keeps the `pnpm setup` command working (just prints a redirect message) instead of silently failing.

    **4. Update package.json:**
    - Keep the `"setup": "tsx scripts/setup.ts"` script (it now prints the redirect message)
    - No other changes needed — seed:demo, create:admin, and other scripts stay

    **5. Verify the starter still builds:**
    Run `npx tsc --noEmit` in the payload-starter directory to confirm payload.config.ts is valid.
  </action>
  <verify>
    <automated>cd C:/Projects/sandbox/payload-starter && npx tsc --noEmit && node -e "const fs = require('fs'); const c = fs.readFileSync('src/payload.config.ts','utf8'); if(c.includes('@setup')) { console.error('Found @setup markers'); process.exit(1); } if(!fs.existsSync('scripts/lib/modify-config.ts')===false) { console.error('modify-config.ts still exists'); process.exit(1); } console.log('Cleanup verified');"</automated>
  </verify>
  <done>
    - payload.config.ts contains zero `@setup` markers and is valid TypeScript
    - scripts/lib/modify-config.ts is deleted
    - scripts/setup.ts prints a redirect message to the CLI package
    - `npx tsc --noEmit` passes in the starter repo
    - All other scripts (seed:demo, create:admin, dev, build) are unaffected
  </done>
</task>

</tasks>

<verification>
1. `cd C:/Projects/sandbox/payload-starter && npx tsc --noEmit` passes
2. No `@setup` markers in payload.config.ts
3. `scripts/lib/modify-config.ts` does not exist
4. `scripts/setup.ts` is a minimal redirect message
</verification>

<success_criteria>
- Starter repo is clean: no @setup markers, no dead code
- Starter builds successfully
- Old comment-toggling approach is fully removed
- Users running `pnpm setup` get directed to the CLI package
</success_criteria>

<output>
After completion, create `.planning/phases/06-developer-experience/06-05-SUMMARY.md`
</output>
